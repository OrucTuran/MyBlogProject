@using Humanizer
@model List<MyBlogNight.EntityLayer.Concrete.Comment>

@{
    Layout = null; 
}
<!--_DefaultBlogDetailsComments-->
@* <div class="custombox clearfix">
    <h4 class="small-title">@Model.Count Yorum</h4>
    <div class="comments-list">
        @foreach (var item in Model)
        {
            <div class="media">
                <a class="media-left" href="#">
                    <img src="~/markedia/markedia/upload/author.jpg" alt="" class="rounded-circle">
                </a>
                <div class="media-body">
                    <h4 class="media-heading user_name">@item.Commenter <small>@item.CreatedDate.Humanize()</small></h4>
                    <p>@item.CommentDetail</p>
                    <a href="#" class="btn btn-primary btn-sm">Reply</a>
                </div>
            </div>
        }
    </div>
</div><!-- end custom-box -->
 *@